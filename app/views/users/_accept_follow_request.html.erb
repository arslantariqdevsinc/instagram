<%model = user.active_relationships.find_by(followed_id: current_user.id)%>
<%= form_with(model: model, remote: true) do |f| %>
  <div><%= hidden_field_tag :status, value: "accepted" %></div>
  <%= f.submit "Accept", class: "btn btn-primary btn-sm mx-2 " %>
<% end %>
