<%= turbo_stream.replace "post_detail#{@post.id}" do%>
  <%= render partial: "posts/post", locals: {post: @post }%>
<%end%>

<%= turbo_stream.replace "posts-feed-#{@post.id}" do%>
  <%= render partial: 'shared/post', locals: {post: @post }%>
<%end%>
