$("#follow_form").html("<%= escape_javascript(render(partial: 'users/follow_form', locals:{user: @user})) %>");
<% if @relationship.accepted? %>
  $("#followers").html('<%= @user.followers.count %>');
<%end%>

